##### Instaling Claude AI Cli Local Windows
``` bat
curl -fsSL https://claude.ai/install.cmd -o install.cmd && install.cmd && del install.cmd
REM ‼ Setup notes:● Native installation exists but C:\Users\prajitgandhi88\.local\bin is not in your PATH. Add it by opening: System Properties → Environment Variables → Edit User PATH → New → Add the path above. Then restart your terminal.
```
