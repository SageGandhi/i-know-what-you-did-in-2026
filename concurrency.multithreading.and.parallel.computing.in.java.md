##### Computer resources are scarce; do not waste CPU or I/O cycles. Concurrency executes multiple threads via time-slicing on a single processor. Parallelism executes threads simultaneously across different processors. Thread Lifecycle(New>Active(Runnable,Ready To Be Executed/Running)>Blocked/Waiting>Terminated).
##### values in heap memory is shared between threads, but stack is associated with specific thread, that is why function without side effect are thread safe. every java object have exactly one intrinsic lock(monitor) with it.two threads running two different synchronized method in same class incrementing different variable will be blocked by other thread.